name: Build Windows

on:
  push:
    paths:
      - 'src/**'
  workflow_dispatch:

permissions:
  contents: write

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build bin
        run: |
          mkdir -p bin
          {
            python -m venv venv
            source venv/Scripts/activate
            pip install --upgrade pip
            pip install pyinstaller
            pyinstaller --onefile src/app.py
            mv dist/app.exe bin/WarpW-windows-bin.exe
          } 2>&1 | tee bin/WarpW-windows-bin.exe.log

      - name: Create branch and push binary
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          BRANCH_NAME="binary-windows-${GITHUB_SHA::7}"
          git checkout -b "$BRANCH_NAME"
          git add bin/WarpW-windows-bin.exe.log
          if [ -f bin/WarpW-windows-bin.exe ]; then
            git add bin/WarpW-windows-bin.exe
          fi
          git commit -m "Add Windows build output (from ${GITHUB_SHA})"
          git push origin "$BRANCH_NAME"